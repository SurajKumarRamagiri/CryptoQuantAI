[pytest]
markers =
    integration: integration test suite
    parity: model parity suite
    db_txn: database transaction suite
    migration: migration checks
    phase3: phase 3 coverage
    trading_core: trading engine coverage
    schema_compat: feature schema compatibility
    model_endpoints: model endpoint tests
    observability: observability checks
    rollback: rollback checks
